<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

<!-- 회원가입 -->
	<insert id="insertMember" parameterType="hashmap" useGeneratedKeys="true" keyProperty="MEMBER_NUMBER">
		<![CDATA[
		INSERT INTO MEMBER (MEMBER_NUMBER, MEMBER_ID, MEMBER_PASSWORD, MEMBER_NAME, MEMBER_NICKNAME, MEMBER_EMAIL, MEMBER_JUMIN, MEMBER_ZIPCODE, MEMBER_ADDRESS1,
					MEMBER_ADDRESS2, MEMBER_PHONE, MEMBER_ADMIN, MEMBER_PROP) 
		VALUES(MEMBER_NUMBER_SEQ.NEXTVAL, #{MEMBER_ID}, #{MEMBER_PASSWORD}, #{MEMBER_NAME},#{MEMBER_NICKNAME}, #{MEMBER_EMAIL}, #{MEMBER_JIMIN}, #{MEMBER_ZIPCODE}, #{MEMBER_ADDRESS1},
					 #{MEMBER_ADDRESS2}, #{MEMBER_PHONE}, '0', #{MEMBER_PROP})
	]]>
	</insert>
	
	<!-- 아이디 중복확인-->
	<select id="checkId" parameterType="String" resultType="int">
	<![CDATA[
		SELECT COUNT(*) FROM MEMBER WHERE MEMBER_ID = #{MEMBER_ID}
	]]>
	</select>
	
	</mapper>